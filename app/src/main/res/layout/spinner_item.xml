<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <TextView
        android:id="@+id/spinnerTextView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="En"
        android:textColor="@color/colorPrimary"
        android:layout_marginStart="@dimen/little_marge"
        android:layout_marginEnd="@dimen/little_marge"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/spinnerImages"
        />

    <ImageView
        android:id="@+id/spinnerImages"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/app_name"
        app:srcCompat="@drawable/ic_united_kingdom_flag_30"
        app:layout_constraintTop_toTopOf="@id/spinnerTextView"
        app:layout_constraintBottom_toBottomOf="@id/spinnerTextView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/spinnerTextView"
        android:layout_marginEnd="@dimen/little_marge"
        />





</androidx.constraintlayout.widget.ConstraintLayout>